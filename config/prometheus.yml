global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090

  # Optional: add node_exporter later if you install it on Mustafar.
  # - job_name: mustafar-node
  #   static_configs:
  #     - targets:
  #         - host.docker.internal:9100

  # Optional: add cAdvisor later for container metrics.
  # - job_name: cadvisor
  #   static_configs:
  #     - targets:
  #         - cadvisor:8080
